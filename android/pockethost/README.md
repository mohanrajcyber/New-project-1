# PocketHost

Android host UI for AahaOS. Package `app.pockethost`.

This is not a hypervisor. Start hands off to Termux.

```bash
# requires Android SDK (see ../../docs/APK.md)
./gradlew :app:assembleDebug
./gradlew :app:bundleRelease
```

Debug APK: `app/build/outputs/apk/debug/app-debug.apk`
